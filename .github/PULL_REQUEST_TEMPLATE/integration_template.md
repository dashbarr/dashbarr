## New integration: [integration name]

<!-- e.g. New integration: Emby -->

A short description of what this integration does and which service or API it connects to.

### Checklist

- [ ] This integration was created using the project generator (`bun run plop integration`).
- [ ] Integration name is in kebab-case (e.g. `my-service`).
- [ ] `bun run build` passes at the repo root (or in `plugins/integrations/<name>`).
- [ ] Lint and typecheck pass (`bun run lint`, `bun run typecheck` or the package equivalents).

### Notes for reviewers

- **API / docs:** <!-- Link to the official API or documentation for the integrated service -->
- **Testing:** <!-- How to test this integration, if applicable (e.g. local server, mock, env vars) -->
