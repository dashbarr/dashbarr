import { Widget } from "@dashbarr/plugin";

export class ClockWidget extends Widget {
    public readonly name = "clock";
    public readonly description = "A widget that displays the current time.";
}
